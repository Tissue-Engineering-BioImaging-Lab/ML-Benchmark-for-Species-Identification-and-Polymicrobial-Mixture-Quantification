# Python dependencies for the Raman-Analysis distributable notebooks
# (RF, XGBoost, CNN, Transformer) and the combine_csvs_by_prefix.py helper.
#
# Install through your CLI:
#     pip install -r requirements.txt
#
# GPU note: the CNN/Transformer notebooks train on GPU through the `raman-cnn-gpu`
# Docker image (NVIDIA NGC TensorFlow, Keras 3) that train_gpu_headless.ps1 uses --
# that image already bundles everything below. Plain pip TensorFlow is CPU-only on
# native Windows (GPU support was dropped after TF 2.10); use the Docker image for GPU.

# --- core data / ML ---
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0

# --- deep learning (CNN + Transformer notebooks) ---
# TensorFlow >= 2.16 ships the Keras 3 API these notebooks are written for
# (LeakyReLU negative_slope=, register_keras_serializable, custom layers).
tensorflow>=2.16

# --- hyperparameter search ---
optuna>=3.4

# --- plotting ---
matplotlib>=3.7
seaborn>=0.13

# --- running / executing the .ipynb notebooks (incl. headless nbconvert) ---
jupyterlab>=4.0
nbconvert>=7.0
ipykernel>=6.0
